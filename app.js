/*function sayhello (name)
{
    console.log('Chal giya ' + name);
}
sayhello('larqay');
console.log(module);


const variable = require('./logger');
//variable = 'changee';
variable.Funcctionlog('malik is awesome');

//console.log(variable);

//EVENT LISTENER

const EventEmitter = require ('events');    //CLASS
const emitter = new EventEmitter();         //OBJECT

//register a listener
emitter.addListener('Message', function(argument){   //we can replace function with arrow (argument) => {}
    console.log('listener called', argument);
});


//Raise an event
emitter.emit('Message', {id: 191, url: 'http://'});

const variable = require('./logger');
const EventEmitter = require ('events');
const emitter = new (EventEmitter);

emitter.on('Mess' );
    console.log('Logging: ');
emitter.emit('Mess');
variable.Funcctionlog('this happened after logging');

require('./prototypee');
require('./logger');
*/